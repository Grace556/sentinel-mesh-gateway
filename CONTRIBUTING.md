# Contributing to Sentinel Mesh Gateway

We welcome contributions to our resilience mesh and gateway modules.

## Quality Standards
- Strict TypeScript enforcement (zero unconstrained any).
- Minimum 85% branch, function, line, and statement test coverage.
- All dependencies verified through npm audit.

## Local Verification Commands
npm ci
npm run typecheck
npm run lint
npm run test
npm run build
