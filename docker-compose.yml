version: '3.8'

services:
  gateway:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=info
      - GATEWAY_PORT=8080
    command: npm run test
